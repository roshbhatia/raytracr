TODO:
	- Fix sphere rendering
	- Implement Z-Buffer Algorithm
	  	- double zbuffer[800][800]
		  	 - initialize all values to something really big (like 10^50)
		- take a look at the zbuffer[xx][yy]
		         - if z value is closer than existing value at point, then you overwrite the value
			      - Then draw G_point(xx,yy)
	- Implement Lightmodel	
		- ambient = constant
		- diffuse = constant * cos(alpha)
		- specular = constant * (cos(beta)^power)
		- cos(alpha) = Nu * Lu
		- cos(beta) = Eu * Ru
	